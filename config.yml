grpc:
  server:
    port: 9001
    gateway:
      enabled: true
      json:
        multiline: false
        indent: ""
        partial: false
        unpopulated: true
        discard: true
        options:
          - enum_as_numbers
          - name_as_proto
